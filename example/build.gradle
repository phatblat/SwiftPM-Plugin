/*
 * build.gradle
 * SwiftPM example
 */

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath group: 'at.phatbl', name: 'swiftpm', version: '+'
    }
}

apply plugin: 'swiftpm'
