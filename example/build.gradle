/*
 * build.gradle
 * SwiftPM example
 */

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath 'at.phatbl:swiftpm:+'
    }
}

apply plugin: 'swiftpm'
